<h1>編集画像</h1>

<%= form_with model: @list, url: update_list_path(@list.id), method: :patch do |f| %>

 <h4>タイトル</h4>
 <%= f.text_field :title %>

 <h4>本文</h4>
 <%= f.text_field :body %>

 <%= f.submit '保存' %>

<% end %>